# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Beads - Exclude local state, keep config
# Pattern: Exclude database/cache, keep templates and committed config
.beads/                     # Exclude entire .beads directory
!.beads/config.yaml         # But include config.yaml if committed
.beads/beads.db*            # SQLite database files (beads.db, beads.db-shm, beads.db-wal)
.beads/issues.jsonl         # Generated JSONL (auto-synced from DB)
.beads/bd.sock              # Daemon socket
.beads/.local_version       # Local version tracking

# Beads template (for distribution)
!beads/config.yaml.example  # Include example config for users

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.goodtogo/
